# notes 索引

> 本目录清单；下钻读子目录 index。只聚合、永不手编，由 index 插件经 `pipeline.py index` 重建。

## concept

- [[notes/代理页的登记与摘要边界]] —— 代理页是资产在 md 世界的代表：登记（`raw_file` / `raw_sha256`）是本职，摘要与结构抽取是可选增强。哈希是失配探测器，不是执法器——…
- [[notes/出身二分]] —— wiki 的页面按出身二分：`wiki/vault/` 下为代理页（有 vault 对应物，路径即出身证明），其余为原生页（出身就在 wiki）。index …
