id: tag
version: 0.6
depends: []
updated: 2026-09-12
attachment:
  - "横向插件：拥有全部页面的 frontmatter tags 字段与词表治理"
  - "scripts/check.py —— 附检（单页 >5 / 复述 type / 层级深度）"
fields:
  tags: "YAML 列表 ≤5；中文为主，英文 kebab-case；层级 父/子 ≤2；禁复述 type"
inject: "页面 `tags` 字段：YAML 列表，中文为主、英文专名小写 kebab-case，层级 `父/子` ≤2，每页 ≤5；开放语义分类，禁止复述 type"
