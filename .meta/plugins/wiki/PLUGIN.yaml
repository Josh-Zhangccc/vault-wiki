id: wiki
version: 0.5
depends: []
updated: 2026-09-22
attachment: []
fields: {}
inject: "wiki 容器 `wiki/`（内外之分的内侧）：出身二分——各域声明属地之页为代理页（有域外对应物，属地路径由各域注入行自披露），其余为原生页（出身在 wiki）；index / tags / hot / log 为派生页（机械投影）；页面 frontmatter 取最小 YAML 子集（顶层标量 / 块列表 / 一级块映射），更复杂结构不受解析"
